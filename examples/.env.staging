# Staging environment.
# Every value is a placeholder; the point is the keys and the syntax.

LOG_LEVEL=debug

# Data stores (service-name hosts, identical across environments)
DATABASE_URL="postgres://app:changeme@db:5432/app"
REDIS_URL=redis://cache:6379/0

# Third parties
STRIPE_API_KEY=sk_test_replace_me
