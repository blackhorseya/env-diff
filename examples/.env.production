# Production environment.
# Same keys as staging in a different order, one value changed, one key
# missing, and one flag left over from an old release.

export DATABASE_URL='postgres://app:changeme@db:5432/app'
export REDIS_URL=redis://cache:6379/0

LOG_LEVEL=info # quieter in production
OLD_FEATURE_FLAG=false
