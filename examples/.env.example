# Keys every environment must define. Values are intentionally empty.
# Check a deployment file against this one with:
#
#   env-diff .env.example .env.production --keys-only

DATABASE_URL=
REDIS_URL=
STRIPE_API_KEY=
LOG_LEVEL=
